name: Deploy documentation site

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    uses: shabaz213/workflow-templates/.github/workflows/mkdocs-doc-site-release.yml@main
